type Mutation {
  parseContract(data: String): String
  go(options: Options): Boolean
}

type Subscription {
  userResult: String
}

input Options {
  sol: String!
  nodeAddress: String!
  noOfUsers: Int!
  contractName: String!
  initialGasCost: Int!
  contractAddress: String
  abi: String
}
